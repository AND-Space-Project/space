const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "db4free.net",
      user: "spacedb",
      password: "(password)",
      database: "spacedb",
    },
    listPerPage: 10,
  };
  module.exports = config;